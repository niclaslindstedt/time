// SPDX-License-Identifier: PolyForm-Noncommercial-1.0.0
import { registerRootComponent } from "expo";

import App from "./App";

// Registers `App` as `main` with the AppRegistry and sets the environment up
// the same way whether the app is loaded in a dev client or a store build.
registerRootComponent(App);
