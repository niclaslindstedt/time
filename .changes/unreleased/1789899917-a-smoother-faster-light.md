---
type: Fixed
title: A smoother, faster light
---

The light that goes round the day is drawn as a turning arc rather than a blurred travelling dash, so it runs at the screen's own rate instead of stuttering — and it comes round every two seconds rather than every five.
