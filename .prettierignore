dist/
node_modules/
coverage/
package-lock.json
# Generated by the release workflow from the `.changes/` fragments — its
# collated sections are machine-written, so they aren't held to Prettier's
# style (and must never be hand-edited to satisfy it).
CHANGELOG.md
# Verbatim copy of the governing spec.
OSS_SPEC.md
# The native wrapper's generated trees: `expo prebuild` output (regenerated
# from native/app.config.js and its config plugins) and the packed web build.
native/ios/
native/android/
native/.expo/
native/assets/
native/node_modules/
# The desktop shell's build output and the site copied into it.
tauri/target/
tauri/webroot/
